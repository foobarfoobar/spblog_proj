<% @title = "editieren" %>
<h1>Favorit bearbeiten</h1>
<!-- <%= form_for(@bookmark) do |f| %> 	way before Ruby 4, works still if using patch instead of put in routes-->
<%= form_for [@bookmark], method: :put do |f| %>
<p>
	<%= f.label :title %>
	<%= f.text_field :title %>
</p>
<p>
	<%= f.label :url %>
	<%= f.text_field :url %>
</p>
<p>
	<%= f.label :comment %>
	<%= f.text_area :comment %>
</p>
<p>
	<%= f.submit %>
</p>
<% end %>
<p><%= link_to "Zurück zur Liste", bookmarks_path %></p>
